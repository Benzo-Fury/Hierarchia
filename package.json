{
  "name": "hierarchia",
  "description": "Sub-millisecond graph layout engine with greedy allocation and poly-relationship support.",
  "keywords": ["tree", "graph", "hierarchy"],
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "bun-plugin-dts": "^0.3.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "compile": "bun run ./scripts/build.ts"
  }
}
